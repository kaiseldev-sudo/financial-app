import { HashLoader, PulseLoader } from 'react-spinners';

export default function LoadingSpinner() {
  return (
    <HashLoader
      color="#059669"
      size={35}
      margin={2}
      speedMultiplier={0.8}
    />
  );
} 